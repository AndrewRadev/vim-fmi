<h1>Потребители</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user do %>
        <%= user_thumbnail user %>
        <div class="name"><%= user.name %></div>
      <% end %>
    </li>
  <% end %>
</ul>

<%= will_paginate @users %>
